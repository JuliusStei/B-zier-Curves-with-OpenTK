namespace OpenGLHandout {
    /// <summary>
    /// the main program to be executed
    /// </summary>
    class Program {
        /// <summary>
        /// entry point of the program
        /// </summary>
        static void Main() {
            
            // create a new window and ...
            OpenGLWindow myWindow = new();

            // ... run the application
            myWindow.Run();
        }
    }
}
